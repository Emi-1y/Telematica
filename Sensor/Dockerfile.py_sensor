FROM python:3.9-slim

WORKDIR /app

COPY sensor.py .

ENV IOT_SERVER_HOST=iot-server
ENV IOT_SERVER_PORT=8080
ENV SENSOR_ID=py-sensor-01
ENV SENSOR_TYPE=temperature

CMD python sensor.py $IOT_SERVER_HOST $IOT_SERVER_PORT $SENSOR_ID $SENSOR_TYPE
